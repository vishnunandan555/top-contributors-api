name: Aggregate Top Contributors

on:
  schedule:
    # - cron: '0 0 * * *'  # Daily   – runs every day at 00:00 UTC
    # - cron: '0 0 * * 1'  # Weekly  – runs every Monday at 00:00 UTC
    - cron: '0 0 1 * *'    # Monthly – runs on the 1st of every month at 00:00 UTC
  workflow_dispatch: # Allows manual triggering from the GitHub UI

permissions:
  contents: write

jobs:
  aggregate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run aggregation script
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_USERNAME: ${{ github.repository_owner }}
        run: node scripts/aggregate.js
        
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git pull --rebase origin main  # Sync with any changes before committing
          git add data/
          # Check if there are any changes before trying to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update top contributors data [skip ci]"
            git push
          fi
